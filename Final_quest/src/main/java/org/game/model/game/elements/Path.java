package org.game.model.game.elements;

import org.game.model.Dialogue.Dialogue;

public class Path extends Element {
    public Path(int x, int y, Dialogue dialogue) {
        super(x, y, dialogue);
    }
}
